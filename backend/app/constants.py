from enum import Enum

CURRENT_EXECUTION_SNAPSHOT_VERSION = 1

class ExecutionStatus(str, Enum):
    PENDING = "pending"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"
    CANCELLED = "cancelled"
